#ifndef BUFFERMANAGER_H
#define BUFFERMANAGER_H

/*
	An universal instance of the Buffer Manager Class will manage the buffer pool.
	It has a hashtable between frame id i.e index and page id.
	A free list of empty frames is maintained.
	A buffer pool is used.
	Maintains a dirty bitset and a pin array corresponding to pages in buffer pool.
*/

#endif